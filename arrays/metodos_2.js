const numeros = [1, 2, 3, 4, 5, 8];

numeros.splice(1, 2); // Ele exclui 2 elemento a partir do índice 1.

console.log(numeros);

numeros.pop(); // Exclui o último elemento.

console.log(numeros);